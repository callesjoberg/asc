name: Release
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest'
            build-cmd: 'mkdir -p resources && clang -O3 -framework Foundation -framework AppKit -framework Vision src-ocr/macos/ocr-helper.m -o resources/ocr-helper-macos'
          - platform: 'windows-latest'
            build-cmd: 'mkdir -p resources && dotnet build -c Release src-ocr/windows/OcrHelper.csproj -o resources/ && cp resources/OcrHelper.exe resources/ocr-helper-win.exe'
    
    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2

      - name: Build OCR Helper
        run: ${{ matrix.build-cmd }}
        shell: bash

      - name: Build App
        run: cargo build --release
        shell: bash

      - name: Upload Release Asset
        run: |
          VERSION=$(grep '^version =' Cargo.toml | head -n1 | cut -d'"' -f2)
          if [ "$GITHUB_REF_TYPE" = "tag" ]; then
            TAG="$GITHUB_REF_NAME"
          else
            TAG="v$VERSION"
          fi

          gh release view "$TAG" >/dev/null 2>&1 || \
            gh release create "$TAG" --title "ASC Screen Analyzer v$VERSION" --draft \
              --notes "Automatisk byggnation av infödda Rust-binärer (.exe och .tar.gz)." || true

          if [ "${{ matrix.platform }}" = "windows-latest" ]; then
            gh release upload "$TAG" target/release/asc-screen-analyzer.exe#ASC_Screen_Analyzer_Portable.exe --clobber
          else
            tar -czf ASC_Screen_Analyzer_mac.tar.gz -C target/release asc-screen-analyzer
            gh release upload "$TAG" ASC_Screen_Analyzer_mac.tar.gz --clobber
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
